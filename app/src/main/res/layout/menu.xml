<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingVertical="15dp"
    android:background="#DCD9D0"
    android:scaleType="fitCenter"
    tools:context=".com.myapplication.myapplication.activity.MenuActivity">

    <!--下单按钮-->
    <ImageButton
        android:id="@+id/btnUpload2"
        android:layout_width="200dp"
        android:layout_height="80dp"
        android:layout_alignParentTop="true"
        android:layout_marginStart="98dp"
        android:layout_marginTop="225dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/upload"
        android:scaleType="fitCenter"
        android:src="@drawable/buy" />



    <!--接单按钮-->
    <ImageButton
        android:id="@+id/btnAccept1"
        android:layout_width="200dp"
        android:layout_height="80dp"
        android:layout_alignParentTop="true"
        android:layout_marginStart="98dp"
        android:layout_marginTop="341dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/accept"
        android:scaleType="fitCenter"
        android:src="@drawable/accept" />

    <!--跳转按钮1-->
    <ImageButton
        android:id="@+id/btnJump1"
        android:layout_width="150dp"
        android:layout_height="80dp"
        android:layout_alignParentTop="true"
        android:layout_marginStart="240dp"
        android:layout_marginTop="0dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/accept"
        android:scaleType="fitCenter"
        android:src="@drawable/square0" />

    <!--跳转按钮2-->
    <ImageButton
        android:id="@+id/btnJump2"
        android:layout_width="150dp"
        android:layout_height="80dp"
        android:layout_alignParentTop="true"
        android:layout_marginStart="240dp"
        android:layout_marginTop="50dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/accept"
        android:scaleType="fitCenter"
        android:src="@drawable/square0" />


</RelativeLayout>